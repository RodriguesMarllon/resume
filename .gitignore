# Compiled PDFs in src/ — snapshot to versions/ instead
src/**/resume.pdf

# LaTeX auxiliary files
*.aux
*.log
*.out
*.toc
*.synctex.gz
*.fdb_latexmk
*.fls

# macOS
.DS_Store

# Tracker — built output and deps
tracker/node_modules
tracker/dist
